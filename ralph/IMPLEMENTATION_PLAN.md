# bodypipe — Implementation Plan

## Phase 1: Pipeline Tabs

- [x] Single-person tab: `views/single_person_tab.py` — drag-drop + browse video input with thumbnail/info, settings group (static_cam, use_dpvo, focal_mm), run/cancel wired to GVHMRWorker, progress bar, output file list with context menus, VideoPlayer preview. AppWindow tab 1 replaced. Tests in `tests/test_single_person_tab.py` (85 total pass).
- [x] Performance capture tab: `views/perf_capture_tab.py` — subclasses SinglePersonTab, adds hand capture group (enable + hybrid/SMPLest-X radios), face capture checkbox. Wired to FullPipelineWorker. AppWindow tab 2 replaced. Tests in `tests/test_perf_capture_tab.py` (105 total pass).
- [x] Multi-person tab shell: `views/multi_person_tab.py` — left sidebar with video input/pipeline settings/multi-person settings (max_persons, confidence_threshold), main viewport with VideoPlayer, vertical+horizontal QSplitters for identity inspector and pose corrector placeholders, TrackOverview with per-person ConfidenceTimeline bars, signal hub wiring frame_changed/person_selected across sub-panels, MultiPersonWorker integration. AppWindow tab 3 replaced. Tests in `tests/test_multi_person_tab.py` (167 total pass).

## Phase 2: Identity Inspector

- [x] Person selector + confidence breakdown + keyframe table + CRUD: `views/identity_inspector.py` — QComboBox person selector with inactive track filtering, ConfidenceTimeline embed, 6 QLabels for confidence breakdown (detection/visibility/overlap/shape/motion/overall with color coding), QTableWidget keyframe table (Frame/Verified/Confidence/BBox/Actions columns with Go/Del buttons), keyframe CRUD (add/remove/verify/prev/next navigation), signal hub (person_changed/frame_requested/keyframe_changed/bbox_overlay_changed). Wired to Session.person_tracks via PersonTrack.keyframes (serialized) and PersonTrack.confidence_breakdown (runtime). MultiPersonTab placeholder replaced, signals connected for frame sync + person selection. Tests in `tests/test_identity_inspector.py` (241 total pass).
- [x] Bbox overlay rendering: `views/bbox_overlay.py` — pure function `render_bbox_overlay()` draws all tracked person bboxes on video frames using cv2. Color-coded by person (PERSON_COLORS), person ID labels with confidence badge, dashed outlines for corrected bboxes, solid for originals. Inactive tracks shown as dashed gray when show_all_tracks=True. Composited onto VideoPlayer in `MultiPersonTab._show_frame()`. Signal wiring: identity_panel.bbox_overlay_changed → show_all toggle, person_changed → redraw, keyframe_changed → redraw. Tests in `tests/test_bbox_overlay.py` (278 total pass).
- [x] Two-click bbox editing: `views/identity_inspector.py` — state machine (idle → click1 → click2 → idle), normalized-to-pixel coordinate mapping via `on_frame_click()`, bbox correction storage with min-size enforcement (20px) and image clamping, automatic keyframe creation, dirty person marking. Edit/Cancel buttons toggle state. Interpolate button for delta-space linear interpolation between corrected keyframes via `interpolate_bbox_corrections()`. Apply All button saves corrections to `person_dir/bbox_corrections.json`. Crosshair preview at corner1 via `render_edit_preview()` in `bbox_overlay.py`. MultiPersonTab wires `frame_clicked → on_frame_click`, stores `edit_preview` state for overlay compositing. Tests in `tests/test_bbox_editing.py` (335 total pass).
- [x] Track operations: `views/identity_inspector.py` — swap IDs (QComboBox target + button, swaps bboxes/confidences/corrections from current frame onward, adds verified keyframes, marks dirty), split track (creates new inactive PersonTrack from frame_idx onward, zeros original after split, splits keyframes/corrections), merge track (QComboBox inactive source + button, non-zero frames overwrite target, removes source, merges keyframes), crossing span marking (Mark Start/Mark End two-click workflow with status label, stored in Session.crossing_spans dict[pid → list[(start, end)]], table display with Del buttons). Show all tracks checkbox already implemented. Session.crossing_spans changed from list[tuple] to dict[int, list[tuple[int, int]]]. `track_modified` signal wired to MultiPersonTab._on_tracks_modified for refresh. Tests in `tests/test_track_operations.py` (407 total pass).
- [x] Review scanner + reprocess: `views/identity_inspector.py` — `ReviewIssue` dataclass + `compute_review_issues()` function scans all active tracks for 4 issue types: low_confidence (spans ≥5 frames below 0.4, severity 0.7), potential_swap (overlap >0.5, severity 0.9), shape_drift (shape confidence <0.4, severity 0.6), track_gap (zero-bbox gaps ≥30 frames, severity 0.5). Scanner UI group: Scan button, Prev/Next issue navigation (wrapping), issues QTableWidget (Frame/Person/Type/Description), status label with [idx/total] format, double-click row to navigate. Reprocess button shows dirty count, emits `reprocess_requested(list)`, wired to `MultiPersonTab._on_reprocess_requested` which launches `ReprocessWorker` with progress/person_done/finished/error signals. Worker completion clears dirty_persons and refreshes all panels. `update_reprocess_button()` auto-called on person_dirty signal. Tests in `tests/test_review_scanner.py` (449 total pass).

## Phase 3: 3D Viewport + Pose Corrector

- [x] QOpenGLWidget mesh viewport: `views/mesh_viewport.py` — MeshViewport(QOpenGLWidget) with graceful degradation when PyOpenGL unavailable (falls back to QWidget label). Lazy SMPL-X model loading via SmplxLite from backend, per-frame vertex computation (torch forward → numpy) with LRU cache (200 entries), per-vertex normal computation, VBO upload (position/normal/color + static EBO), Phong shading using existing shaders/mesh.vert + mesh.frag. Camera projection from session camera_K or estimated K via k_to_projection() with CV→GL coordinate flip. Pure helper functions: compute_normals(), k_to_projection(), estimate_K(). Integrated into MultiPersonTab replacing placeholder — wired to frame_changed, person_selected, track_clicked signals. Tests in `tests/test_mesh_viewport.py` (500 total pass).
- [x] Camera modes: `views/mesh_viewport.py` — in-camera mode using K_fullimg intrinsics (existing k_to_projection), free orbit mode with mouse controls (left drag=orbit, middle drag=pan, scroll=zoom, double-click=reset). Pure helper functions `compute_orbit_view()` (spherical look-at matrix) and `perspective_fov()` (symmetric FOV projection). Orbit state: yaw/pitch/distance/center with auto-centering on mesh centroid (CV→GL coordinate flip). Model matrix set to _CV_TO_GL in orbit mode so orbit operates in GL space. Mode-aware `_update_projection()` switches between K-based and FOV-based projection. `camera_changed` signal emitted on all camera state changes. Tests in `tests/test_mesh_viewport.py` (544 total pass).
- [x] Skeleton overlay + joint picking: `views/mesh_viewport.py` — skeleton data constants (JOINT_NAMES, JOINT_PARENTS, DEFAULT_OFFSETS, BONE_CONNECTIONS — 52-joint SMPL-X hierarchy from smplx_to_bvh.py). Pure helper functions: `forward_kinematics()` (axis-angle FK using scipy.spatial.transform.Rotation, supports body+hand poses), `project_joints_to_screen()` (MVP projection → NDC → screen pixels), `find_nearest_joint()` (screen-space distance hit-test, body joints only by default). GL rendering: bones drawn as GL_LINES (light gray, 2px width), joints as GL_POINTS (white body / gray hand, 6px), selected joint in accent color (#e94560, 12px). Skeleton drawn on top of mesh (depth test disabled). Joint picking on left-click via `_pick_joint()` → `joint_clicked(int)` signal. `highlight_joint(idx)` sets selected joint and triggers repaint. `set_show_skeleton(bool)` toggles overlay. `_compute_joints()` called in `_refresh_mesh()` alongside vertex computation. Tests in `tests/test_mesh_viewport.py` (591 total pass).
- [x] Pose corrector joint controls: `views/pose_corrector_panel.py` — PoseCorrectorPanel(QWidget) with embedded MeshViewport, camera/color mode dropdowns, person selector (QComboBox populated from session.person_tracks), joint selector (QComboBox with all 52 SMPL-X joints + separator between body/hand, synced from viewport joint_clicked signal), 3 euler sliders (QSlider + QDoubleSpinBox, range ±180°, step 0.1°). Real-time mesh preview via MeshViewport.set_pose_override() (temporary per-frame param override that skips vertex cache and applies modified copy to both _compute_vertices and _compute_joints). Apply button commits correction to CorrectionTrack from GVHMR backend (creates/merges PoseCorrection keyframes, updates raw smplx_params in session, persists to correction_track.json). Reset Joint removes single joint from correction, Reset All removes entire frame correction. Pure helper functions: get_joint_euler() reads rotation from corrections then raw params, _get_joint_axis_angle() handles all 52 joints (global_orient/body_pose/left_hand_pose/right_hand_pose), fallback euler conversions when backend unavailable. MeshViewport additions: set_pose_override(dict|None), invalidate_cache(person_id, frame_idx), _apply_override_to_params(). MultiPersonTab updated: PoseCorrectorPanel wraps MeshViewport in bottom splitter, person/frame changes propagated through pose corrector. Signals: joint_selected(int), correction_applied(int,int). Tests in `tests/test_pose_corrector.py` (663 total pass).
- [x] Quick-fix buttons + corrections table: `views/pose_corrector_panel.py` — Quick Fix QGroupBox with 4 buttons: Flip Body (180° rotation via QInputDialog axis selection yaw/pitch/roll, calls backend flip_global_orient or scipy fallback), Invert (180° pitch flip for upside-down poses), Mirror L/R (swaps 8 L/R joint pairs via _LR_SWAP_PAIRS using backend mirror_lr_pose or fallback), Copy From (QInputDialog for source frame, copies global_orient + all 21 body_pose joints). All quick-fix ops use shared `_apply_quick_fix()` helper that merges with existing corrections, updates raw params, invalidates viewport cache, refreshes table. Corrections table: QGroupBox with QTableWidget (5 columns: Frame/Type/Joint/Go/Del), populated from CorrectionTrack.corrections sorted by frame, Go button emits `frame_requested(int)` signal (wired to video_player.seek in MultiPersonTab), Del button removes correction and refreshes. Table auto-refreshes on apply/reset/quick-fix/person-change. Pure fallback functions: `flip_global_orient_fallback()` (scipy Rotation compose), `mirror_lr_pose_fallback()` (sparse dict swap), `_safe_import_quick_fix()` for lazy backend import. `_describe_correction_joints()` builds readable joint summary with truncation. `frame_requested` signal added, connected in MultiPersonTab._connect_signals. Tests in `tests/test_pose_corrector.py` (708 total pass).
- [x] Space overrides + BVH/FBX export: `views/pose_corrector_panel.py` — Space Overrides QGroupBox with QComboBox (World/Camera/Carried), start/end QSpinBoxes, reference person QComboBox (populated from active tracks, excludes inactive), Y offset QDoubleSpinBox (default 0.4m), Add Override/Delete Selected buttons, QTableWidget (5 columns: Start/End/Space/Ref Person/Y Offset). CRUD via CorrectionTrack.add_space_override/remove_space_override using lazy-imported FrameSpaceOverride from backend. Export QGroupBox with Re-export BVH button (calls convert_params_to_bvh with corrections + reference_params for carried overrides) and Re-export FBX button (BVH + Blender subprocess via convert_bvh_to_fbx). Graceful degradation when backend unavailable. `export_requested(str)` signal ("bvh"/"fbx") emitted on success. Space table auto-refreshes on person change. Lazy import helpers: `_safe_import_space_override()`, `_safe_import_bvh_export()`, `_safe_import_fbx_export()`. Tests in `tests/test_pose_corrector.py` (741 total pass).

## Phase 4: App Shell Polish

All spec-defined menu actions are wired and functional. Without session save/load, users lose all corrections/keyframes on close — this is the most critical gap.

- [x] Session save/load + menu completion: `app_window.py` — Wire File > Save Session (Ctrl+S) to Session.save() with auto-path ({output_dir}/bodypipe_session.json) or QFileDialog fallback. Wire File > Open Session (Ctrl+Shift+O) to Session.load() with field-copy to shared session object + tab video reload. Recent Sessions submenu (QSettings-backed, max 5 entries, dynamic population, stale-path removal). Help > About dialog (QMessageBox). View > Toggle Status Bar (checkable action). Tests in `tests/test_app_window.py` (784 total pass).
- [x] Undo/redo stack: `models/session.py` — UndoEntry dataclass + UndoStack class (push/undo/redo/clear/peek_undo/peek_redo, max 50 depth, on_changed callback). Session.undo_stack field (transient, not serialized). AppWindow Edit > Undo (Ctrl+Z) / Redo (Ctrl+Shift+Z) wired with dynamic action text ("Undo Swap IDs", etc.), status bar feedback, enabled-state sync via on_changed. Identity inspector integration: verify/add/remove/delete keyframe, bbox correction, interpolate bboxes, swap IDs, split track, merge track, add/delete crossing span — all push UndoEntry with snapshot-based undo/redo closures using _snapshot_track/_restore_track helpers. Pose corrector integration: apply correction, reset joint, reset all, quick-fix (flip/invert/mirror/copy), delete correction, add/delete space override — all push UndoEntry with _snapshot_correction/_restore_correction helpers and raw param backup. Session load preserves undo stack callback, clears history. Tests in `tests/test_models.py` (797 total pass).
- [x] Keyboard shortcuts dialog: `views/keyboard_shortcuts_dialog.py` — KeyboardShortcutsDialog(QDialog) with QTableWidget (3 columns: Category/Shortcut/Action), SHORTCUTS registry constant (14 entries across File/Edit/View/Video categories), read-only non-editable table, Close button via QDialogButtonBox. Wired to Help > Keyboard Shortcuts in app_window.py via `_on_keyboard_shortcuts()`. Tests in `tests/test_keyboard_shortcuts_dialog.py` (58 total pass with app_window tests).

## Phase 5: Spec Compliance Gaps

- [x] VideoPlayer right-click context menu: `views/video_player.py` — FrameDisplay right-click context menu with Copy Frame (copies current displayed frame to system clipboard as full-resolution QPixmap) and Save Frame As... (QFileDialog save to PNG/JPG). FrameDisplay stores frame copy on set_frame() for clipboard/save operations, uses _frame_to_pixmap() helper for RGB→QPixmap conversion. CustomContextMenu policy with _show_context_menu() handler. Tests in `tests/test_video_player.py` (30 tests covering FrameCache, FrameDisplay context menu, copy/save operations, VideoPlayer construction/seeking). (842 total pass).
- [x] Status bar frame/FPS wiring + VideoPlayer FPS label: `views/video_player.py`, `app_window.py`, `views/single_person_tab.py`, `views/multi_person_tab.py` — VideoPlayer slider row now shows FPS label ("FPS: 30.0") alongside frame counter per spec diagram. Added `fps` and `num_frames` properties to VideoPlayer. Each tab exposes `video_player` property for external access. AppWindow wires each tab's video player `frame_changed` signal to status bar updates (frame counter + FPS), with active-tab filtering so only the current tab's player drives the display. Tab switch handler refreshes status bar from new tab's player state (or clears when no video loaded). Tests in `tests/test_video_player.py` (5 new: FPS label existence, FPS on video load, fps/num_frames properties) and `tests/test_app_window.py` (7 new: tab video_player property for all 3 tabs, frame change updates status bar, inactive tab ignored, tab switch updates, tab switch clears). (853 total pass).
- [x] Multi-person viewport switching + signal wiring: `views/multi_person_tab.py`, `views/pose_corrector_panel.py` — QStackedWidget in main viewport area with Video/3D Mesh toggle toolbar (QToolButton pair). Index 0: VideoPlayer with bbox overlay (default). Index 1: dedicated MeshViewport for large 3D view (separate from PoseCorrectorPanel's embedded viewport). Toolbar buttons mutually exclusive (checked state management via `_switch_to_video()`/`_switch_to_mesh()`). Missing signal wiring completed: `main_mesh_viewport.joint_clicked` → `pose_corrector.set_joint()` (new public method), `frame_changed` → `main_mesh_viewport.on_frame_changed()`, person selection → `main_mesh_viewport.set_person()` (from both track click and identity inspector). `viewport_stack` property exposed for testing. PoseCorrectorPanel gained public `set_joint(int)` method per spec contract. Tests in `tests/test_multi_person_tab.py` (15 new: TestViewportSwitching 12 tests + TestMeshViewportSignalWiring 4 tests). (868 total pass).

- [x] Splitter layout persistence: `views/multi_person_tab.py` — All three QSplitters (main horizontal, vertical viewport/panels, bottom identity/pose) save/restore state via QSettings under `multi_person/` keys. `_save_splitter_state()` called on `splitterMoved` signal from each splitter, `_restore_splitter_state()` called on init. `main_splitter` promoted to instance attribute (`self._main_splitter`) for persistence access. Tests in `tests/test_multi_person_tab.py` (7 new: TestSplitterPersistence — settings instance, main_splitter attr, save writes settings, round-trip restore, missing settings handled, splitterMoved triggers save, all three splitters connected). (1001 total pass).

- [x] Multi-person pipeline settings parity: `models/pipeline_config.py`, `views/multi_person_tab.py`, `workers/pipeline_orchestrator.py` — Added 4 missing Gradio GUI settings to PipelineConfig (target_fps, fbx_naming, render_overlays, use_inpainting) with defaults matching Gradio (30 FPS, Mixamo naming, overlays off, inpainting on). MultiPersonTab gains: Target FPS QDoubleSpinBox (1-120, step 1), FBX Naming QComboBox (Mixamo/UE5), SAM2+ProPainter inpainting QCheckBox (default on), Render per-person overlays QCheckBox (default off). All 4 controls wired through get_config/set_config round-trip, disabled during pipeline run via _set_running(). MultiPersonWorker now passes render_overlays + use_inpainting to backend split_multi_person_video(). Bug fix: removed invalid gvhmr_dir kwarg from split_multi_person_video() call (backend doesn't accept it). Tests in test_models.py (4 new: defaults, round-trip, to_dict, from_dict), test_multi_person_tab.py (4 new: default widgets + 4 updated settings tests + 2 updated running state tests), test_workers.py (1 new: config stores new fields). (1010 total pass).

### Known remaining spec gaps (lower priority)

- [x] 3D viewport color modes: `views/mesh_viewport.py` — `set_color_mode()` fully implemented with three modes. **Solid**: skin-tone (default). **Joint**: per-vertex coloring from LBS weights argmax with 22-color body palette, hand joints inherit wrist color, jaw/eye joints inherit head color via `compute_joint_colors()`. **Confidence**: red→yellow→green gradient from per-frame confidence (reads `confidence_breakdown["overall"]` then falls back to raw `confidences` list) via `confidence_to_color()`. Colors recomputed in `_compute_colors()` called from `_upload_buffers()`. LBS weights extracted from SmplxLite model on load. PoseCorrectorPanel color dropdown already wired. Tests in `tests/test_mesh_viewport.py` (28 new: TestComputeJointColors 7, TestConfidenceToColor 8, TestMeshViewportColorMode 13). (896 total pass).
- [x] 3D viewport grid floor: `views/mesh_viewport.py` — pure `compute_grid_lines()` function generates XZ-plane grid vertices (±10m, 20 divisions per half-axis) with center-line highlighting via `_GRID_AXIS_COLOR` vs `_GRID_COLOR`. `MeshViewport` renders grid in orbit mode only via `_draw_grid()` (GL_LINES, depth-tested, unlit). Grid Y auto-positioned at lowest mesh vertex (feet level) during `_auto_center_orbit()`. `set_show_grid(bool)` toggles visibility. Tests in `tests/test_mesh_viewport.py` (17 new: TestComputeGridLines 11, TestMeshViewportGrid 6). (913 total pass).
- [x] 3D viewport joint name text overlay: `views/mesh_viewport.py` — QPainter text overlay drawn after GL rendering in `paintGL()`. Pure helper `compute_joint_label_layout()` computes screen-space label positions from projected joints (body-only by default, selected joint always shown + sorted last for z-order). Labels rendered with semi-transparent background, accent-colored for selected joint. `set_show_joint_labels(bool)` public API, guarded by `_show_skeleton` flag. PoseCorrectorPanel gains "Labels" checkbox wired to embedded viewport's `set_show_joint_labels()`. Tests in `tests/test_mesh_viewport.py` (TestComputeJointLabelLayout 12 + TestMeshViewportJointLabels 5) and `tests/test_pose_corrector.py` (2 new). (930 total pass).
- [x] FullPipelineWorker stages 3-6: `workers/pipeline_orchestrator.py` — Stage 3 (Merge): finds GVHMR `hmr4d_results.pt` via `find_output_dir`/`find_file`, extracts params via `extract_gvhmr_params`, merges with SMPLest-X hands via `merge_gvhmr_smplestx_params` (world + camera coordinate spaces), saves merged .pt via `save_merged_pt()`. Falls back to GVHMR-only when SMPLest-X unavailable. Stage 4 (Face): conditional on `config.use_face`, runs `run_face_pipeline()` for ARKit blendshape CSV + `render_face_mesh_video()` for mesh overlay. Uses `extract_bboxes_from_output()` or `fallback_face_bboxes()` for face crop regions, finds ViTPose data for improved crops. Stage 5 (BVH/FBX): `convert_params_to_bvh()` (hybrid: skip world grounding, smooth hands only) or `convert_smplx_to_bvh()` (GVHMR-only), then `convert_bvh_to_fbx()` via Blender. Stage 6 (Rendering): `render_skeleton_video()`, `render_world_views()`, `render_hand_overlay_video()` with per-stage progress callbacks via `_stage_progress_callback()`. All stages use lazy imports, try/except for graceful degradation when backend modules unavailable, and cancellation checks. Module-level pure helpers: `find_smplestx_result()`, `save_merged_pt()`, `extract_bboxes_from_output()`, `fallback_face_bboxes()`. Tests in `tests/test_workers.py` (21 new: TestFindSmplestxResult 5, TestSaveMergedPt 3, TestExtractBboxesFromOutput 5, TestStageProgressCallback 3, TestMergeStageNoOutput 2, TestBvhFbxStageNoParams 1, TestRenderingStageNoParams 1, TestFaceStageDisabled 1). (951 total pass).
- [x] Pose corrector auto-detect bad spans: `views/pose_corrector_panel.py` — `PoseIssue` dataclass + `compute_pose_issues()` pure function scans active person tracks for 3 issue types: angular_jump (per-joint angular change > 45° between consecutive frames via vectorized `_rotation_angles_deg()`, severity = angle/180°), jitter (sliding-window std of max angular velocity > 20° for ≥3 frames, severity 0.6), low_confidence (spans of ≥3 frames below 0.4, severity 0.7, handles both float lists and TrackConfidence objects). Auto-Detect QGroupBox on right panel: Detect Bad Spans button, Prev/Next issue navigation (wrapping), issues QTableWidget (4 columns: Frame/Person/Type/Description), status label with [idx/total] format, double-click row to navigate, `frame_requested` signal emits for seek. Issues sorted by severity (desc) then frame (asc). Tests in `tests/test_pose_corrector.py` (43 new: TestRotationAnglesDeg 5, TestDetectAngularJumps 7, TestDetectJitter 4, TestDetectLowConfidence 7, TestComputePoseIssues 8, TestAutoDetectUI 12). (994 total pass).

## Phase 6: Remaining Gradio Parity

- [x] Perf capture pipeline settings parity: `views/perf_capture_tab.py`, `models/pipeline_config.py`, `workers/pipeline_orchestrator.py` — Added 6 missing Gradio Tab 2 settings. PipelineConfig gains 4 new fields: pitch_adjust (float, default 0.0, range ±30°), hand_source (str, "smplestx"|"hamer"), body_smooth_preset (str, "light"|"moderate"|"heavy"), use_vitpose_face_crops (bool, default True). PerfCaptureTab gains: Target FPS QDoubleSpinBox (1-120, default 30), FBX Naming QComboBox (Mixamo/UE5), Pitch Adjust QDoubleSpinBox (±30°, step 0.5°), Hand Source radios (SMPLest-X/HaMeR) inside hand capture group, Body Smoothing QComboBox (Light/Moderate/Heavy), Use ViTPose face crops QCheckBox. All controls wired through get_config/set_config round-trip, disabled during pipeline run via _set_running override. _on_run passes config.target_fps to worker fps param. FullPipelineWorker updated: _run_bvh_fbx passes config.pitch_adjust (pitch_adjust_deg kwarg, SMPLest-X only mode), config.body_smooth_preset (body_smooth_preset kwarg), config.fbx_naming → naming key conversion ("ue5"/"mixamo"). _run_face uses config.use_vitpose_face_crops to gate ViTPose usage. _try_hamer helper for HaMeR hand source (lazy import, graceful fallback). Tests in test_perf_capture_tab.py (17 new: construction, defaults, config, running state), test_models.py (4 new: defaults, round-trip, to_dict, from_dict), test_workers.py (7 new: config passthrough, naming conversion, HaMeR fallback). (1033 total pass).
- [x] Multi-person post-pipeline FBX conversion: `workers/pipeline_orchestrator.py` — `MultiPersonWorker._convert_bvh_to_fbx_batch()` scans `result.person_dirs` for BVH files and converts each to FBX via `convert_bvh_to_fbx()` with the user-chosen naming convention (`fbx_naming` config → "ue5"/"mixamo" key) and `target_fps`. Runs after `split_multi_person_video()` completes, using progress band [0.90, 0.98]. Graceful degradation: skips existing FBX files, handles missing `bvh_to_fbx` import, per-file exception handling so one failure doesn't block others, cancellation support. FBX paths returned in `finished` signal dict as `fbx_files` key. Tests in `tests/test_workers.py` (14 new: TestMultiPersonFbxBatch — BVH discovery, UE5/Mixamo naming, FPS passthrough, skip existing, empty/missing dirs, import error, per-file error, ERROR log string, progress emission, cancellation, result dict). (1047 total pass).
